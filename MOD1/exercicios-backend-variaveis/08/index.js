const PI = 3.14;

let raio = 1;
let comprimento = 2 * PI * raio;
let area = PI * raio ** 2;

console.log(`Valor de PI: ${PI}, valor do raio: ${raio}, comprimento do circulo: ${comprimento.toFixed(1)}, área do circulo: ${area}`);