const arrayA = [5, 32, 3, 44, 1];
const arrayB = [57, 4, 21, 2, 13];
const LIMIT = 5;

for (let index = 0; index < LIMIT; index++) arrayA[index] < arrayB[index] ? console.log(arrayA[index]) : console.log(arrayB[index]);
